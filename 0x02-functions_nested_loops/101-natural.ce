#include <stdio.h>
/**
 * main - main function
 *
 * Return: always 0
 */
int main(void)
{
	int l;
	int s = 0;

	for (l = 0; l < 1024; l++)
	{
		if (l % 3 == 0 || l % 5 == 0)
		{
			s += l;
		}

	}
	printf("%d\n", s);
	return (0);
}
